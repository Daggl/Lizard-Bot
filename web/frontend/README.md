Frontend (React + Vite) for Bot Dashboard

Run (development):

```bash
cd web/frontend
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:8000` (same origin is assumed in dev via proxy or CORS).

MVP: allows entering a guild ID and loading/saving JSON config.
